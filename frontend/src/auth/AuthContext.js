import { createContext } from 'react'

const AuthContext = createContext({
    // user: null, 
    // token: null, 
    // setToken: () => {}
})

export default AuthContext

// Following example from https://www.makeuseof.com/react-context-api-tutorial/